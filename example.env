# Secret key for Django (use a strong key in production)
SECRET_KEY=django-insecure-by21%oz+$xr1@wudt5!te#(rj@s=it09zj%az!u&-y-%f9!qj1

# Django environment setting (development, production, etc.)
DJANGO_ENV=development

# Allowed Hosts - Specify your domain(s) here
ALLOWED_HOSTS=your-production-domain.com,www.your-production-domain.com

# Database Configuration (PostgreSQL for production)
DATABASE_URL=postgres://your-db-user:your-db-password@your-db-host:5432/your-db-name

# Email Configuration (replace with your email provider details)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.your-email-provider.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@example.com
EMAIL_HOST_PASSWORD=your-email-password

# Use this to specify additional production settings for various services like caching or file storage
CACHE_URL=redis://your-redis-host:6379/0  # Example for Redis caching
DEFAULT_FILE_STORAGE=django.core.files.storage.FileSystemStorage  # Replace with S3 for production

# For production security, consider using a secure session cookie for HTTPS
SESSION_COOKIE_SECURE=True
CSRF_COOKIE_SECURE=True

# If using Cloud storage, API keys, or third-party services, configure them here
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_STORAGE_BUCKET_NAME=your-bucket-name

# Logging Configuration (optional, adjust for production logging)
LOGGING_LEVEL=ERROR
LOGGING_FILE_PATH=/path/to/your/logfile.log
